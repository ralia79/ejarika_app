import 'package:flutter/material.dart';

class NewAdScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        body: Center(
      child: Text('new ad screen'),
    ));
  }
}
